# Copy to .env — never commit .env
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY

# Server-side only (edge functions — set in Supabase dashboard, not here)
# BASIQ_API_KEY=
